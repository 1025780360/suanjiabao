{
  "app_dir": "backend",
  "build_command": "python manage.py collectstatic --noinput",
  "start_command": "python manage.py migrate --noinput && python manage.py ensure_admin && gunicorn garment_costing.wsgi:application --bind 0.0.0.0:${PORT:-8080}"
}
